[package]
name = "fluxo-plugins"
edition.workspace = true
version.workspace = true
license.workspace = true

[features]
default = ["boringssl"]
boringssl = ["fluxo-core/boringssl"]
rustls = ["fluxo-core/rustls"]

[dependencies]
fluxo-core = { path = "../fluxo-core", default-features = false }
